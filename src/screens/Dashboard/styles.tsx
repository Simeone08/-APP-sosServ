import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
    },
    ImageContainer: {
    justifyContent: 'center',
    alignItems: 'center',
    },
    button: {
        width: '80%',
        height: 80,
        marginTop: 80,
        borderRadius: 6,
        marginBottom: 20,

        justifyContent: 'center',
        alignItems: 'center',

        backgroundColor: '#000',
    },
    text: {
        color: '#fff',
        fontSize: 24,
        borderRadius: 4,
    },
});